<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <% if(locals.details){%>
      <h2>
        <%=details.name%>
      </h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%=details.ingredients.protein.name%>, <%=details.ingredients.protein.preparation%></li>
        <li><%=details.ingredients.salsa.name%></li>
        <%for(let i=0;i<details.ingredients.toppings.length;i++){%>
          <li><%=details.ingredients.toppings[i].quantity%>, <%=details.ingredients.toppings[i].name%></li>
        <%}%>   
      </ul>
      <%} else{ %>
        <h2>Pick your favourite taco ingredient 👆</h2>
       <% } %>
  
  </div>
</body>

</html>